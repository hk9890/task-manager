<!--
type: feature — a capability that does not exist yet.

Use when   the capability does not exist and someone has asked for it.
Inside     who wants it and what they do with it; the user-visible behaviour once
           it exists; the edge of this slice — what is explicitly out.
Not inside internal design that has not actually been decided. An invented
           structure reads as a requirement and the implementer will build it.
           State constraints instead, and let the design be made where it is made.

A finished body to match the register is in taskmgr guide filing.

Delete this comment block before filing.
-->

## Context

<Where it lands: the file, the page, the endpoint. Who asked, and what they do
today instead — that is what makes the priority defensible.>

## Problem

<What is not possible now, and what it costs the person who wants it. Frequency
and effort belong here: "support runs the query by hand, twice a week, twenty
minutes each time".>

## Recommended action

<The user-visible behaviour once this is built. Reuse what already exists rather
than adding a parallel path, and say which.

Out of this slice: <the neighbouring work that is explicitly not here. Each is its
own issue if it turns out to be wanted.>>

## Acceptance criteria

- [ ] <The main path, observable by someone who was not there>
- [ ] <The constraint that must not break — scoping, permissions, an existing flow>
- [ ] <The test command that covers it>
