/bin/
/dist/
coverage.out
*.test
.DS_Store
.idea/

# taskmgr task store (generated by `taskmgr init`)
.tasks/

# Feature worktrees (docs/CHANGE-WORKFLOW.md creates them here, inside the
# repository, so without this rule each one is an untracked nested checkout).
.claude/

# Go workspace sum (the committed go.work is dev-only; consumers ignore it)
go.work.sum
