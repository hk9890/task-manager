/bin/
/dist/
/bench/bench
coverage.out
*.test
.DS_Store
.idea/

# taskmgr task store (generated by `taskmgr init`)
.tasks/

# Go workspace sum (the committed go.work is dev-only; consumers ignore it)
go.work.sum

# Local private working notes — never publish.
hako-private/
